server:
  port: 19095

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/asset_db}

aws:
  region: ${AWS_REGION:ap-northeast-2}
  s3:
    bucket: ${AWS_S3_BUCKET:money-town-assets}