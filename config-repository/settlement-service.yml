server:
  port: 19097

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/settlement_db}
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true